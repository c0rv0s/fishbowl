# Pearl Seahorse posture and scale

September 9, 2026.

The seahorse's live scale is reduced from 0.80 to 0.64. Its head sits over the chest, with a recessed throat, raised neck, sloping brow, and gently lowered snout. Small pectoral fins now attach beneath the head. The tail's four curve segments and taper are unchanged.

The mouth position follows the new snout for feeding. Widget framing also gives the seahorse more space, and the snapshot cache revision refreshes existing previews.

Validation: Simulator app and widget build succeeded. Existing catalog checks passed geometry, normals, shaders, and feeding alignment. Existing widget checks passed all populations and layouts. The final scene and large widget render were visually reviewed. The app was restored to live animation after the diagnostic captures. The pre-existing app/extension version mismatch warning remains.

- `after.png`: actual Simulator scene at the resting diagnostic camera.
- `seahorse-widgetSmall.png`, `seahorse-widgetMedium.png`, `seahorse-widgetLarge.png`: final widget renders.
- `catalog-check.log`, `widget-check.log`: validation results.
